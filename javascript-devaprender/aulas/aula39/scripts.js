// Combinando e cortando arrays

const primeiro = [1,2,3]
const segundo = [4,5,6]

// Combinar

const combinado = primeiro.concat(segundo)

console.log(combinado)

// Dividir

const cortado = combinado.slice(1,2)
console.log(cortado)

// exercicio

const um = ['a','b','c']
const dois = ['d','e','f']

// adicionando

const adc = um.concat(dois)
console.log(adc)

// dividindo

const dividindo = adc.slice(1,3)
console.log(dividindo)