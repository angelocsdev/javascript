let num = 11

let res = (num % 2 == 0 ? "DEU PAR" : "DEU ÍMPAR")

console.log(res)

let num1 = 19
let num2 = 15

res = (num1 > num2 ? "Verdadeiro" : "Falso")

console.log(res)

let st = "Aaaaa"
res = (st == "A" ? "ATIVO" : "INATIVO")
console.log(res)